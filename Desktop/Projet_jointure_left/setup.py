from setuptools import setup, find_packages

setup(
    name="Spark version finale",  # Name of your project
    version="0.1",
    packages=find_packages(),  # Automatically finds all packages (directories with __init__.py)
)
